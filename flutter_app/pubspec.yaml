name: isp_customer_portal
description: Central do Assinante Multi-tenant para ISPs
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.0
  shared_preferences: ^2.3.0
  flutter_secure_storage: ^9.2.0
  google_fonts: ^6.2.0
  intl: ^0.19.0
  flutter_svg: ^2.0.0
  cached_network_image: ^3.4.0
  loading_animation_widget: ^1.2.0
  url_launcher: ^6.3.0
  video_player: ^2.8.2
  chewie: ^1.7.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
